# Environment
NODE_ENV=development

# Security (CHANGE THESE IN PRODUCTION)
JWT_SECRET=your-secret-key-change-in-production

# Override defaults.json settings if needed (optional)
# See Web/server/src/config/defaults.json for all available configuration
# NODE_ENV=production
# PORT=3000
# DATABASE_PATH=./db
